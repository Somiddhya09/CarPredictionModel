🚗 Car Price Prediction using Random Forest

This project builds a machine learning model to predict the price of a car based on its specifications.
It uses the Car Price Dataset (205 records, 26 features) and applies One-Hot Encoding for categorical variables and a Random Forest Regressor for prediction.
